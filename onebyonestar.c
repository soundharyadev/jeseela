#include <stdio.h>
 
int main()
{
    int n, a, b;
 
    printf("Enter number of rows\n");
    scanf("%d",&n);
 
    for ( a = 1 ; a <= n ; a++ )
    {
        for( b = 1 ; b <= c ; b++ )
            printf("*");
 
        printf("\n");
    }
 
    return 0;
    }
